var str = 'This is a string.'; 

//length method - returns size of the string
var strLen = str.length;
// sqauare bracket notation - go to charachter at specified string index 
var strIdx = str[8];

// index of method - get: position of character in string
var strIdx = str.indexdf(5);


// slice method - gets a substring or specified portion of a string.center
var strSlice = str.slice(5,7)

// toUpperCase() and toLowerCase() - change the case of a string charater 
var strUpper = str.toUpperCase();
var strLower = str.toLowerCase();

// replace() method - replace chararter of a string
var strRpl = str.replace('a', 'our');


//console.log(strRpl);

//Arrays

var fruits= ['apples', 'oranges', 'grapes', 'lemons'];

// array method 

//length method - gets the number of elements in an array 
    //console.log(fruits.length);

//push () - adds new element into array 
 //   fruits.push('cantelop') 
 //   console.log(fruits);
 //   fruits.pop('cantelop');
 //   fruits.pop(fruits);


 // sort method - sorts the elements in the array 
    console.log(fruits);
    console.log(fruits.sort())

//toString() method - converts an array to a string and returns the result
console.log(fruit);
console.log(fruit )